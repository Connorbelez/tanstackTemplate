#!/usr/bin/env sh
set -e

staged_files="$(git diff --cached --name-only --diff-filter=ACMR)"

if [ -z "$staged_files" ]; then
	exit 0
fi

bunx ultracite fix $(printf '%s\n' "$staged_files" | sed 's/ /\\ /g')
git update-index --again
