Starting CodeRabbit review in plain text mode...

Review directory: fairlendapp

Connecting to review service
Setting up
Preparing sandbox
Summarizing
Tools completed
Reviewing

============================================================================
File: convex/payments/transfers/providers/rotessa.ts
Line: 44 to 54
Type: potential_issue

Comment:
Risky fallback to first row when no match is found.

If no transaction matches by transaction_number or id, falling back to rows[0] could return status for the wrong transaction. This could lead to incorrect status being reported for a providerRef that doesn't exist in the window.

Consider throwing an error when the specific transaction isn't found rather than falling back to an arbitrary row:



🐛 Proposed fix to remove risky fallback

 		const matched =
 			rows.find(
 				(row) =>
 					row.transaction_number === providerRef ||
 					String(row.id) === providerRef
-			) ?? rows[0];
+			);
 		if (!matched) {
 			throw new Error(
 				Rotessa transaction "${providerRef}" was not found in the transaction report window.

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @convex/payments/transfers/providers/rotessa.ts around lines 44 - 54, The current lookup assigns matched = rows.find(...) ?? rows[0], which risks returning the wrong transaction when no match exists; change the logic to remove the fallback to rows[0] so matched is strictly the result of rows.find(...) and, if matched is falsy, throw a clear Error including providerRef (e.g., keep the existing throw message) instead of falling back—update the code around the matched variable/rows.find and the subsequent if (!matched) branch to enforce this behavior.

============================================================================
File: convex/payments/recurringSchedules/queries.ts
Line: 27 to 33
Type: potential_issue

Comment:
Fallback to first borrower when no primary exists may silently select the wrong party.

If no primary role exists, the code falls back to borrowerLink[0], which could be a co-borrower or guarantor. Consider explicitly requiring a primary borrower or logging when the fallback is used.

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @convex/payments/recurringSchedules/queries.ts around lines 27 - 33, The current fallback of assigning primaryBorrowerLink = borrowerLink.find(... ) ?? borrowerLink[0] can silently pick the wrong party; update the logic in the block that sets primaryBorrowerLink so it does not fall back to borrowerLink[0]. Instead, after computing borrowerLink.find((link) => link.role === "primary") assign to primaryBorrowerLink and if it is undefined throw a ConvexError (include args.mortgageId and a brief serialized borrowerLink or count) indicating no explicit primary borrower; alternatively if you must keep a fallback, replace the fallback with a processLogger.warn that logs the fallback and the borrowerLink contents before using borrowerLink[0]. Ensure the change is applied where primaryBorrowerLink, borrowerLink, ConvexError, and args.mortgageId are referenced.

============================================================================
File: convex/payments/recurringSchedules/queries.ts
Line: 35
Type: potential_issue

Comment:
Consider making asOf required or documenting the time-dependent behavior.

The Date.now() fallback on line 35 means snapshot results vary based on when the query is invoked, not a fixed point in time. Since activateRecurringSchedule already accepts an optional asOf, requiring it would ensure deterministic snapshot behavior. Alternatively, if the default "as of now" is intentional, document this clearly for callers.

Prompt for AI Agent:
Verify each finding against the current code and only fix it if needed.

In @convex/payments/recurringSchedules/queries.ts at line 35, The current fallback const asOf = args.asOf ?? Date.now(); in queries.ts makes snapshot results time-dependent; either make asOf required by changing the activateRecurringSchedule (and its args/type) to require an asOf timestamp (remove the ?? Date.now() fallback and update callers) or explicitly document the time-dependent behavior by adding JSDoc on activateRecurringSchedule and the args.asOf property stating that asOf defaults to Date.now() when omitted and that snapshots are taken at invocation time; update any related type definitions and callers to reflect the chosen approach.

Review completed: 3 findings ✔
