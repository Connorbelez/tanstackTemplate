# Chunk: chunk-01-schema-provider

- [x] T-010: Extend `convex/schema.ts` and `convex/documents/contracts.ts` for normalized signature tables and signing fields.
- [x] T-020: Add the backend signature provider seam and Documenso adapter using existing `documensoConfig` output.
