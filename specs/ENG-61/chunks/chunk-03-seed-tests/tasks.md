# Chunk 3: Seed + Tests + Validation

- [ ] T-009: Create `convex/payments/collectionPlan/seed.ts` — idempotent seed mutation for 3 default rules
- [ ] T-010: Create `convex/payments/__tests__/rules.test.ts` — rules engine + individual rule tests
- [ ] T-011: Run full validation: `bun check` + `bun typecheck` + `bunx convex codegen`
