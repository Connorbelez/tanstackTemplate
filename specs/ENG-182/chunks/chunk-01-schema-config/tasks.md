# Chunk 01: Schema & Configuration

- [ ] T-001: Add `payoutFrequency`, `lastPayoutDate`, `minimumPayoutCents` fields to `lenders` table in `convex/schema.ts`
- [ ] T-002: Create `convex/payments/payout/config.ts` — frequency types, `isPayoutDue()` logic, constants
- [ ] T-003: Create `convex/payments/payout/validators.ts` — `payoutFrequencyValidator`
