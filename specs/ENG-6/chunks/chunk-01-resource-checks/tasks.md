# Chunk 01: resource-checks

- [ ] T-001: Verify `closingTeamAssignments` table exists in `convex/schema.ts` with correct fields (`mortgageId`, `userId`, `role`, `assignedBy`, `assignedAt`) and indexes (`by_mortgage`, `by_user`). Already defined per ENG-18 — confirm no changes needed.
- [ ] T-002: Create `convex/auth/resourceChecks.ts` with imports, Viewer type reference, and shared helper `getLenderMortgageIds(ctx, lenderId)` that queries `ledger_accounts` by `lenderId` index for POSITION accounts with positive balance, returning `Set<Id<"mortgages">>`.
- [ ] T-003: Add helper `isBrokerForMortgage(ctx, viewer, mortgageId)` that fetches the mortgage and checks `brokerOfRecordId` or `assignedBrokerId` against the broker profile linked to `viewer.authId`.
- [ ] T-004: Implement `canAccessMortgage(ctx, viewer, mortgageId)` — admin shortcut via `viewer.isFairLendAdmin`, borrower check via `mortgageBorrowers` join table (query by_borrower → filter mortgageId, requires looking up borrower by `viewer.authId`), broker check via `isBrokerForMortgage`, lender check via `getLenderMortgageIds`, lawyer check via `closingTeamAssignments` by_user index filtered by mortgageId.
- [ ] T-005: Implement `canAccessDeal(ctx, viewer, dealId)` — admin shortcut, broker check (fetch deal → deal.mortgageId → `isBrokerForMortgage`), lender check (deal.buyerId/sellerId match viewer.authId), lawyer check (closingTeamAssignment for deal's mortgage OR dealAccess record with active status).
- [ ] T-006: Implement `canAccessLedgerPosition(ctx, viewer, mortgageId)` — admin shortcut, lender check via `getLenderMortgageIds`, broker check via `isBrokerForMortgage`.
- [ ] T-007: Implement `canAccessAccrual(ctx, viewer, investorId)` — admin shortcut, lender check (`investorId === viewer.authId`), broker check (look up lender by userId matching investorId's user, check lender.brokerId matches viewer's broker profile).
- [ ] T-008: Implement `canAccessDispersal(ctx, viewer, investorId)` — admin shortcut, lender check (`investorId === viewer.authId`), no broker access.
- [ ] T-009: Implement `canAccessDocument(ctx, viewer, documentId)` — stub with TODO for ENG-144 (generatedDocuments table not yet defined). For now, admin check + return false for non-admin. Add comment explaining the dependency.
- [ ] T-010: Implement `canAccessApplicationPackage(ctx, viewer, packageId)` — admin shortcut, sr_underwriter always true, jr_underwriter/underwriter see pool (status === "assembled") and own claims (status === "under_review" && machineContext.claimedBy === viewer.authId), `underwriting:review_decisions` permission sees `decision_pending_review`.
- [ ] T-011: Search codebase for remaining `investor` role string references (NOT schema field names like `investorId` or `lenderId`). Rename any `investor` role checks to `lender`. Rename function names like `getInvestorPositions` → `getLenderPositions` if any remain.
- [ ] T-012: Search codebase for any remaining `isPlatformAdmin` references and rename to `isFairLendAdmin`. Search for `uw_manager` role references and rename to `sr_underwriter`.
