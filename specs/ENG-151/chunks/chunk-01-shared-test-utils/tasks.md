# Chunk 01: Shared Test Utilities

## Tasks
- [ ] T-001: Create `testUtils.test.ts` with shared constants (SYSTEM_SOURCE, ADMIN_SOURCE, ADMIN_IDENTITY)
- [ ] T-002: Implement `createHarness()` factory and type export
- [ ] T-003: Implement `seedMinimalEntities()` — seeds mortgage, lender, borrower, obligation for testing
- [ ] T-004: Implement `createTestAccount()` — creates cash_ledger_accounts by family with optional subaccount
- [ ] T-005: Implement `postTestEntry()` — convenience wrapper around postCashEntryInternal for tests
